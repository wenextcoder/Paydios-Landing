"use client";

import { FileText, LogIn, Menu, X, ChevronDown } from "lucide-react";

export const Icons = {
  fileText: FileText,
  logIn: LogIn,
  menu: Menu,
  x: X,
  chevronDown: ChevronDown,
};